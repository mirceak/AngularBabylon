import { PageFormlyDirective } from './page-formly.directive';

describe('PageFormlyDirective', () => {
  it('should create an instance', () => {
    const directive = new PageFormlyDirective(null);
    expect(directive).toBeTruthy();
  });
});
